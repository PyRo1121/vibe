# Domain name research (archived)

One-off scripts used to scout TCG-related domain names. Not part of the runtime app.

Run manually from repo root if needed:

```powershell
npx tsx docs/tcg-vault/branding/check-domains-rdap.ts
```

Do not commit `check-domains-result.json` — it is gitignored.
