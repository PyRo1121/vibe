# Workspace note

This folder is a Cursor workspace shell. The TCG Vault app lives in the monorepo at:

**`../apps/tcg-vault`**

From the repo root (`Vibe/`):

```powershell
cd apps/tcg-vault
npm run dev
# or from root:
npm run dev:tcg-vault
```

Open **`C:\Users\olen\Documents\Coding\Vibe`** as your workspace for the full monorepo (Preflight + TCG Vault).
